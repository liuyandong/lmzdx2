Find me in app/views/phones/index.html.erb
<p id="notice"><%= notice %></p>

<h1>列出你知道的手机名称</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @phones.each do |phone| %>
      <tr>
        <td><%= phone.name %></td>
        <td><%= link_to 'Show', phone %></td>
        <td><%= link_to 'Edit', edit_phone_path(phone) %></td>
        <td><%= link_to 'Destroy', phone, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Phone', new_phone_path %>

	<br />
	<br />
	<p>第九次视频讲的内容讲了Rails框架的大体概况，可以在下面几个链接中找到:</p>
	<%= link_to 'view中生成变量', controller: 'credit_cards', action: 'open'%>
