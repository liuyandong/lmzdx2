<%provide(:title, @user.name)%>

<div class="container">
<table class="table">
	<br />
  <thead>
    <tr>
      <th scope="col">ID</th>
      <th scope="col">用户名</th>
      <th scope="col">电子邮件</th>
      <th scope="col">哈希加密后的密码</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <th scope="row"><%= @user.id %></th>
      <td><%= @user.name %></td>
      <td><%= @user.email %></td>
      <td><%= @user.password_digest %></td>
    </tr>
  </tbody>
</table>
<h2>你已经成功建立了这个账户，如果想查看所有用户列表，请点击下面这个链接: <%= link_to "所有用户列表", users_path %></h2>

</div>
</div>